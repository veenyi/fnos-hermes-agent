# fnos-hermes-agent v0.21.102 → v0.21.142 更新日志

## v0.21.102 — 基线版本
- Hermes Agent fnOS 应用初始稳定版
- 基础功能：对话/群聊/专家/模型/扩展/通讯/知识库/定时/记忆/用量/更新/设置
- 通讯渠道：Telegram/Discord/Slack/WhatsApp/Matrix/飞书/钉钉/QQ/微信/企业微信
- 专家系统：267 中文专家角色 + Agency Orchestrator 60+ 工作流预设
- 群聊：多 Agent 协作房间，@ 成员并行回复

---

## v0.21.103 ~ v0.21.142 — 迭代改进

### 🔴 通讯渠道全面优化
- **QQ 机器人扫码绑定**：q.qq.com 官方二维码扫码流程（create_bind_task → poll_bind_result → AES-GCM 解密凭证）
- **QQ 多账号支持**：适配器改造支持 5 个 QQ 机器人同时在线（子实例并发连接，各自 WebSocket/Token）
- **QQ 机器人 2-5 独立扫码**：每个槽位可分别扫码绑定不同机器人
- **微信配置补全**：CDN Base URL / 私信策略 / 群组策略 / 白名单 / Home Channel / 多行拆分（10 字段，对齐 Hermes 官方文档）
- **企业微信扫码恢复**：qrLogin 标记修复
- **新增 10 个渠道**：Signal / 腾讯元宝 / BlueBubbles / Google Chat / SMS(Twilio) / Email / Mattermost / Teams / Home Assistant / Webhooks（共 20 个渠道）
- **官方文档指南链接**：每个渠道卡片直达 Hermes 官方文档 + 平台文档
- **群消息配置**：QQ group_policy/dm_policy 自动设为 open
- **STT 语音转写**：QQ 机器人支持 GLM-ASR / Whisper 配置
- **通道会话同步**：QQ/微信等已绑定通道显示在对话会话树"通道会话"区

### 🟣 群聊系统增强
- **AI 自主接力（DGA 进化）**：AI 主持人每轮停顿思考 → 基于上文现场选下一位发言人 → 给出选择理由 → 自动接续（默认 5 轮，最大 20，手动激活/停止）
- **流式 Markdown 渲染**：群聊回复边生成边按 Markdown 格式渲染（不再纯文本后转）
- **消息操作按钮**：每条回复下方新增"📋 复制"（保留格式）+ "🔄 刷新"（重新生成）
- **@ 成员微信风格**：@ 后输入框保留"@角色名"标签，消息带 @ 名字
- **发送者名显示**：群聊消息显示真实角色名 + 时间戳（从消息数据取，不依赖会话状态）
- **房间列表折叠**：复用全局折叠按钮（标题旁），群聊页折叠房间列表
- **历史消息时间**：使用消息自身时间戳（刷新后不变）
- **消息渲染容错**：逐条 try/catch，单条失败不中断整个列表
- **重复消息修复**：发送后不再本地+广播双重渲染
- **🛠 [object Object] 修复**：工具调用名称正确提取

### 🔵 主对话优化
- **@ 专家微信风格**：@ 后输入框保留"@专家名"，消息带 @ 标记
- **@ 专家一次性语义**：@ 专家回复一条后自动恢复默认角色（不再持久绑定）
- **发送者角色名**：回复消息顶部加粗显示角色名（如"地理学家"而非"Hermes"）
- **消息数据 sender 字段**：后端存消息时写入角色名，刷新后历史消息正确显示
- **@ 专家列表可滚动**：285 位专家全部渲染（不再 slice 截断为 8 个）
- **群聊 @ 浮层统一**：搜索框 + 紧凑列表 + 小气泡样式（与主对话一致）

### 🟢 知识库改进
- **知识图谱修复**：无链接时环形布局（整齐不重叠），节点点击打开笔记
- **Tab 切换修复**：图谱/轨迹 Tab 正确隐藏文档树（!important 强制）
- **布局锁定**：#kbLayout 强制左右 flex（不再上下/左右跳变）
- **页面显示修复**：移除 #page-kb display:flex（不再在所有页面右侧出现知识库）
- **收录记忆按钮**：一键把 MEMORY.md/USER.md/notes.md + 每日记忆沉淀进知识库
- **轨迹/图谱 Tab 激活**：切换页面时正确触发渲染

### 🟡 记忆系统
- **每日日记自动记录**：群聊成员回复完成后自动写入 memory/YYYY-MM-DD.md
- **Curator 自进化间隔**：从 7 天改为 24 小时
- **立即运行自进化按钮**：记忆页可手动触发 hermes curator run
- **Guardrails 风格统一**：安全护栏从复选框改为开关（toggle），与设置页其他项一致

### 🟠 模型服务
- **冒烟测试下拉修复**：模型列表正确填充（同步读 _cfg，兼容对象/字符串数组）
- **选择保留**：周期重填不重置用户当前选择
- **CodeBuddy Bridge [object Object] 修复**：模型下拉兼容对象数组

### 🔧 系统级改进
- **.env 读取 bug 修复**：_getEnvValue 正则 \s 匹配换行导致空值误读下一行键（所有渠道"清空后仍显示已配置"根因）
- **配置路径修复**：平台配置读写改为运行时 profile config（网关实际加载的文件）
- **自动任务拆分思维链**：写入 AGENTS.md，AI 自主判断是否拆分→并行子 Agent→验证→报告（不需要用户说"帮我拆分"）
- **4 个仓库集成**：superpowers-zh（20 技能）/ ai-coding-guide / shellward-compliance / local-agent-toolkit
- **会话树护栏**：群聊等非对话页硬性隐藏对话会话树（display:none + hashchange 监听）
- **扫码成功自动关闭**：网关重启就绪后弹窗自动关闭（30s 上限兜底）

### 🐛 Bug 修复汇总
- 清空配置后仍显示"已配置"（_getEnvValue \s 正则 bug）
- 企业微信扫码按钮丢失（qrLogin 标记被覆盖）
- STT 字段误判为必填（全角括号"（可选）"未匹配）
- 消息重复显示（本地追加 + SSE 广播双重渲染）
- 工具调用显示 [object Object]（对象未提取名称）
- 历史消息发送者名显示"Hermes"（消息无 sender 字段）
- 知识库布局跳变（#page-kb display:flex 覆盖隐藏）
- 群聊历史消息时间为当前时间（忽略消息 ts）
- 模型冒烟测试下拉为空（异步时序 + 缓存）
- 主对话 @ 专家列表不可滚动（slice 截断）
- 通道会话不显示 QQ/微信（Dashboard API 认证失败 + 未补充已配置通道）
- QQ 群不回复（group_policy 默认 pairing → 改为 open + 写入 profile config）

---

**版本号说明**：v0.21.102 为基线，v0.21.103~v0.21.142 为迭代改进版本。所有改动通过 fnpack 打包为 .fpk，可通过 fnOS 应用中心安装升级。
